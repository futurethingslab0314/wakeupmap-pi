# Wakeup Map Pi 語言與 Notion 雙語資料整合執行計畫書

## 目標

本次調整的目標如下：

1. Raspberry Pi 介面可透過 `.env` 切換主體語言。
2. Pi 介面可支援 `LANGUAGE=ZH` 與 `LANGUAGE=EN`。
3. Notion 資料庫固定保存中英文雙語資料，不因 Pi 顯示語言而缺漏。
4. 問候語 `greeting` 維持當地語言的「早安」，不受 `LANGUAGE` 切換影響。
5. 語音播放第一句永遠先播放當地語言的 `greeting`，後續故事段落才依 `LANGUAGE` 切換。
6. 清理 Pi 介面上的多餘符號與地圖縮放按鈕。

## 核心規則

### 1. 顯示語言與資料儲存分離

`LANGUAGE` 只控制：

- Pi 畫面文字顯示語言
- 故事文字顯示語言
- 第二段故事語音的語言

`LANGUAGE` 不控制：

- `greeting`
- Notion 中英文雙語欄位是否寫入

### 2. 城市與國家名稱的資料來源優先順序

城市與國家名稱的雙語資料，應優先使用專案內部城市 `json` 檔中的原始欄位。

建議優先順序如下：

1. 先讀取內部 `json` 既有欄位
2. 若 `city_zh` 或 `country_zh` 已存在，直接使用
3. 若 `city_zh` 或 `country_zh` 缺值，則以英文 `city` / `country` 作為 AI 翻譯來源
4. 翻譯後結果補入：
   - `city_zh`
   - `country_zh`
5. 最終寫入 Notion 時，英文與中文欄位都必須完整保留

此規則代表：

- 已經有中文資料的城市，不需要重複呼叫 AI
- 只有缺漏中文資料時，才使用 AI 作為 fallback
- 英文原文必須始終保留，不可被中文顯示流程覆蓋

### 3. Notion 欄位固定雙語保存

不論 Pi 畫面目前顯示中文或英文，Notion 都必須固定保存：

- `city`: 英文城市名
- `country`: 英文國家名
- `city_zh`: 中文城市名
- `country_zh`: 中文國家名
- `greeting`: 當地語言早安
- `story`: 英文故事
- `story_zh`: 中文故事
- `longtitude`: 經度數字
- `latitude`: 緯度數字

### 4. 問候語與語音規則

- `greeting` 永遠是當地語言版本
- 語音第一句永遠先講 `greeting`
- 第二段故事依 `.env` 的 `LANGUAGE` 決定：
  - `LANGUAGE=ZH`：播放 `story_zh`
  - `LANGUAGE=EN`：播放 `story`

## `.env` 規格

Pi 端新增：

```env
LANGUAGE=ZH
```

可用值：

- `ZH`
- `EN`

建議預設：

```env
LANGUAGE=ZH
```

## 預期使用體驗

### 當 `LANGUAGE=ZH`

- Pi 介面顯示中文城市、中文國家
- Pi 故事文字顯示 `story_zh`
- 語音先播放當地語言 `greeting`
- 語音後半段播放中文故事 `story_zh`

### 當 `LANGUAGE=EN`

- Pi 介面顯示英文城市、英文國家
- Pi 故事文字顯示 `story`
- 語音先播放當地語言 `greeting`
- 語音後半段播放英文故事 `story`

## 需要調整的功能區塊

### 1. 前端 `pi-script.js`

需要完成：

- 新增讀取 `window.env.LANGUAGE`
- 建立顯示語言選擇函式
- 將畫面上的城市/國家顯示切成：
  - 中文模式顯示 `city_zh` / `country_zh`
  - 英文模式顯示 `city` / `country`
- 將故事顯示切成：
  - 中文模式顯示 `story_zh`
  - 英文模式顯示 `story`
- 確保前端送到 `/api/save-record` 的 payload 同時帶：
  - 英文城市國家
  - 中文城市國家
  - 英文故事
  - 中文故事
  - `latitude`
  - `longitude`
  - `longtitude`
- 移除城市名稱後方多餘的 `:`
- 移除 `+` / `-` 地圖按鈕

### 2. Vercel API `api/save-record/index.js`

需要完成：

- 優先接收內部 `json` 提供的英文與中文城市資料
- 寫入 Notion 前驗證 `city` / `country` 是否為英文原文
- 若 `city_zh` / `country_zh` 缺漏，使用英文 `city` / `country` 做 AI 翻譯補齊中文欄位
- 若 `story` / `story_zh` 任一缺漏，依現有故事來源補齊
- 嚴格寫入：
  - `city`
  - `country`
  - `city_zh`
  - `country_zh`
  - `story`
  - `story_zh`
  - `latitude`
  - `longtitude`

### 3. Raspberry Pi Python 流程

主要檔案：

- `raspberrypi-dsi/main_web_dsi.py`
- `raspberrypi-dsi/audio_manager.py`

需要完成：

- 將 `LANGUAGE` 從 Pi `.env` 讀入
- 保留故事資料中的英文版與中文版
- `piStoryReady` 注入前端時同時帶入：
  - `city`
  - `country`
  - `city_zh`
  - `country_zh`
  - `story`
  - `story_zh`
  - `latitude`
  - `longitude`
- 音訊組裝改為：
  - 第一段：`greeting`
  - 第二段：依 `LANGUAGE` 選 `story` 或 `story_zh`

## 故事生成策略

目前方向應調整為雙版本故事：

- `story`: 英文版故事
- `story_zh`: 中文版故事

建議做法：

1. 在故事生成 API 一次產出英文與中文兩個版本。
2. 若目前後端只有中文故事，先補成雙語輸出。
3. 前端與 Pi 播放端都不要自行把中文覆蓋英文欄位。

## 城市資料翻譯策略

城市與國家名稱的翻譯策略，應明確區分「原始資料」與「補救翻譯」：

1. 原始資料來源為專案內部城市 `json`
2. 若 `json` 已提供：
   - `city`
   - `country`
   - `city_zh`
   - `country_zh`
   則直接使用，不再翻譯
3. 若 `json` 只提供英文欄位，缺少中文欄位：
   - 用英文 `city` 與 `country` 呼叫 AI 翻譯
   - 生成 `city_zh` / `country_zh`
4. AI 翻譯只作為 fallback，不作為主要來源
5. 不可從 Pi 畫面上的顯示文字回推英文欄位，避免資料污染

## UI 清理項目

### 必做

- 移除城市名稱後的 `:`
- 移除右側地圖 `+` / `-` 按鈕

### 驗收結果

- Pi 畫面上的城市名稱純文字顯示，不帶冒號
- 介面右側不再顯示地圖縮放控制按鈕

## 驗收清單

### A. `LANGUAGE=ZH`

驗收條件：

- 按下按鈕後畫面立即切到 `LOCATING...`
- 結果頁城市與國家顯示中文
- `greeting` 仍是當地語言
- 故事文字顯示中文
- Notion 中同一筆資料包含：
  - `city` 英文
  - `country` 英文
  - `city_zh` 中文
  - `country_zh` 中文
  - `story` 英文
  - `story_zh` 中文
  - `latitude`
  - `longtitude`

### B. `LANGUAGE=EN`

驗收條件：

- 按下按鈕後畫面立即切到 `LOCATING...`
- 結果頁城市與國家顯示英文
- `greeting` 仍是當地語言
- 故事文字顯示英文
- Notion 中同一筆資料仍包含完整雙語欄位

### C. 翻譯 fallback

驗收條件：

- 若原始資料沒有中文地名
- 系統可自動用 AI 產出 `city_zh` / `country_zh`
- Notion 中文欄位不可空白

## 建議實作順序

1. 補齊故事資料結構，定義 `story` 與 `story_zh`
2. 補齊城市國家資料結構，定義 `city` / `country` 與 `city_zh` / `country_zh`
3. 將 `LANGUAGE` 接入 Pi 前端與 Pi 語音流程
4. 強化 `/api/save-record` 的欄位寫入驗證
5. 清掉 Pi 畫面的 `:` 與 `+/-`
6. 以 `LANGUAGE=ZH`、`LANGUAGE=EN` 各測一次

## 風險與注意事項

- 目前專案中仍有部分舊邏輯會用畫面文字反推資料，容易把中文覆蓋英文欄位
- 若故事生成端仍只產中文，則 `story` 會持續缺失
- 若前端與 Python 後端送出的 payload 欄位名稱不一致，經緯度仍可能漏寫
- Notion 欄位名稱 `longtitude` 為既有拼法，實作時需維持相容，避免寫入失敗

## 完成定義

以下條件全部成立，才算完成：

1. Pi 可透過 `.env` 切換 `LANGUAGE=ZH` / `LANGUAGE=EN`
2. `greeting` 永遠是當地語言
3. `story` 與 `story_zh` 皆可寫入 Notion
4. `city` / `country` 與 `city_zh` / `country_zh` 皆可寫入 Notion
5. 經緯度可穩定寫入 Notion
6. Pi 畫面不再顯示城市後方冒號
7. Pi 畫面不再顯示 `+` / `-` 地圖按鈕
