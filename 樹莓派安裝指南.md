# 🍓 Raspberry Pi 甦醒地圖安裝指南

## 📋 **安裝前準備**

### **檢查當前狀態**
```bash
# SSH 連接到樹莓派
ssh yutingpi@raspberrypi

# 查看當前項目狀態 (如果已有舊版本)
cd ~/wakeupmap-pi 2>/dev/null || cd ~/pi/subjective-clock 2>/dev/null || echo "首次安裝"
pwd
ls -la
```

### **備份現有版本** (如果需要)
```bash
# 如果已有舊版本，進行備份
if [ -f "pi.html" ]; then
    cp pi.html pi.html.backup.$(date +%Y%m%d_%H%M%S)
    echo "✅ 已備份現有版本"
fi
```

---

## 🚀 **安裝方法：Git 下載 (推薦)**

### **步驟 1: 下載或更新專案**
```bash
# 如果是首次安裝
cd ~
git clone https://github.com/yutingcheng/wakeupmap-pi.git
cd wakeupmap-pi

# 如果是更新現有版本
cd ~/wakeupmap-pi
git pull origin main

# 檢查是否成功下載
git log --oneline -3
```

### **步驟 2: 驗證核心文件**
```bash
# 檢查必要文件是否存在
ls -la pi.html pi-script.js pi-style.css

# 應該看到:
# pi.html          - 主介面文件
# pi-script.js     - 核心邏輯腳本  
# pi-style.css     - 樣式文件
# cities_data.json - 城市資料庫

# 檢查 API 配置
ls -la api/config/
```

### **步驟 3: 安裝 Python 依賴**
```bash
# 更新系統套件
sudo apt update && sudo apt upgrade -y

# 安裝基本 Python 環境
sudo apt install python3 python3-pip python3-venv -y

# 進入 GPIO 控制程式目錄並安裝依賴
cd raspberrypi-dsi
pip3 install -r requirements.txt

# 檢查安裝是否成功
python3 -c "import RPi.GPIO, selenium, pygame; print('✅ 主要依賴安裝成功')"
```

### **步驟 4: 選擇使用模式**

此專案支援兩種使用模式：

#### **模式 A: 純網頁版本** (無需實體按鈕)
```bash
# 僅使用網頁界面，適合：
# - 觸控螢幕操作
# - 鍵盤快捷鍵控制
# - 測試和展示用途
echo "選擇純網頁模式：僅需開啟瀏覽器即可使用"
```

#### **模式 B: GPIO 按鈕版本** (需要實體按鈕)
```bash
# 使用實體按鈕控制，適合：
# - 完整的實體裝置體驗
# - 自動啟動和後台運行
# - 日常使用的儀式感

# 執行 GPIO 控制程式安裝腳本
cd raspberrypi-dsi
chmod +x install_web_dsi.sh
./install_web_dsi.sh
```

---

## ✅ **安裝驗證**

### **步驟 1: 檢查文件結構**
```bash
# 在樹莓派上檢查完整結構
cd ~/wakeupmap-pi
ls -la

# 應該看到以下核心文件:
# pi.html           - 主介面文件
# pi-script.js      - 主要邏輯腳本
# pi-style.css      - 樣式文件
# cities_data.json  - 城市資料庫
# api/              - API 配置目錄
# *.png, *.ico      - 圖標文件

# 檢查 API 配置
ls -la api/config/index.js
```

### **步驟 2: 測試 Web 服務器**
```bash
# 啟動簡單的 Python HTTP 服務器測試
cd ~/wakeupmap-pi
python3 -m http.server 8000 &

# 記錄進程 ID，方便後續關閉
echo $! > web_server.pid
echo "✅ Web 服務器已啟動在 http://localhost:8000"
```

### **步驟 3: 瀏覽器測試**
```bash
# 在樹莓派本地瀏覽器中訪問
# http://localhost:8000/pi.html

# 或者從其他設備訪問樹莓派:
# http://raspberrypi.local:8000/pi.html
# 或 http://[樹莓派IP]:8000/pi.html

# 檢查頁面是否正常顯示:
# ✅ 應該看到 "PRESS THE BUTTON" 主畫面
# ✅ 地圖背景正常載入
# ✅ 沒有 JavaScript 錯誤 (按 F12 檢查控制台)
```

### **步驟 4: 停止測試服務器**
```bash
# 測試完成後停止服務器
if [ -f web_server.pid ]; then
    kill $(cat web_server.pid)
    rm web_server.pid
    echo "🔴 測試服務器已停止"
fi
```

---

## 🔧 **使用模式說明**

根據您在步驟 4 的選擇，有兩種使用方式：

### **模式 A: 純網頁版本控制**
```bash
# 適用於：測試、展示、觸控螢幕使用

# 1. 螢幕觸控
# 直接點擊頁面上的隱藏按鈕 (左上角，幾乎透明)

# 2. 鍵盤快捷鍵
# - Ctrl + H : 直接觸發 startTheDay() 功能
# - ESC     : 顯示緊急按鈕 5 秒 (左上角)

# 3. 瀏覽器控制台
# 按 F12 開啟控制台，輸入: window.startTheDay()
```

### **模式 B: GPIO 實體按鈕控制**
```bash
# 適用於：日常使用、完整裝置體驗

# 啟動 GPIO 控制程式
cd ~/wakeupmap-pi/raspberrypi-dsi
python3 main_web_dsi.py

# 或使用自動啟動服務 (如果已設定)
sudo systemctl start wakeupmap

# 按下實體按鈕即可觸發功能
```

### **確認控制功能**

#### **測試網頁版本**
```bash
# 1. 在瀏覽器中開啟 pi.html
# 2. 按 Ctrl + H
# 3. 應該看到頁面從 "PRESS THE BUTTON" 切換到 "LOCATING..."
```

#### **測試 GPIO 版本** (如果選擇了模式 B)
```bash
# 1. 確認 GPIO 控制程式運行中
ps aux | grep main_web_dsi

# 2. 按下實體按鈕
# 3. 檢查終端輸出是否顯示按鈕觸發訊息
```

---

## 🧪 **功能測試**

### **測試基本頁面功能**
```bash
# 在瀏覽器中打開 pi.html 後，按 F12 開啟控制台測試
```

### **測試 JavaScript 載入**
```javascript
// 在瀏覽器控制台中執行以下測試:

// 1. 檢查核心函數是否存在
console.log('核心函數檢查:', {
    startTheDay: typeof window.startTheDay,
    StateManager: typeof window.StateManager,
    mainInteractiveMap: !!window.mainInteractiveMap
});

// 2. 檢查當前狀態
console.log('當前狀態:', window.currentState);

// 3. 檢查地圖是否載入
console.log('地圖狀態:', {
    leafletLoaded: typeof L !== 'undefined',
    mapContainer: !!document.getElementById('mainMapContainer'),
    mapInitialized: !!window.mainInteractiveMap
});
```

### **測試快捷鍵功能**
```bash
# 在頁面上測試以下快捷鍵:
# 1. 按 ESC 鍵     → 應該臨時顯示左上角緊急按鈕 5 秒
# 2. 按 Ctrl + H  → 應該觸發 startTheDay() 函數
```

### **測試手動觸發功能**
```javascript
// 在瀏覽器控制台中手動觸發:

// 1. 觸發開始功能
if (window.startTheDay) {
    window.startTheDay();
} else {
    console.error('startTheDay 函數未找到');
}

// 2. 顯示/隱藏緊急按鈕
if (window.hiddenButtonControl) {
    window.hiddenButtonControl.show();  // 顯示
    // window.hiddenButtonControl.hide();  // 隱藏
    // window.hiddenButtonControl.help();  // 幫助
} else {
    console.error('hiddenButtonControl 未找到');
}
```

### **測試 GPIO 按鈕功能** (模式 B 用戶)
```bash
# 1. 檢查 GPIO 控制程式狀態
cd ~/wakeupmap-pi/raspberrypi-dsi
python3 main_web_dsi.py --status

# 2. 測試按鈕連接
# 執行以下指令會監聽按鈕狀態
python3 -c "
import RPi.GPIO as GPIO
import time
GPIO.setmode(GPIO.BCM)
GPIO.setup(18, GPIO.IN, pull_up_down=GPIO.PUD_UP)
print('按下按鈕進行測試...')
for i in range(10):
    if GPIO.input(18) == GPIO.LOW:
        print('✅ 按鈕觸發成功！')
        break
    time.sleep(1)
GPIO.cleanup()
"

# 3. 檢查音頻輸出
# 測試是否能正常播放聲音
aplay /usr/share/sounds/alsa/Front_Left.wav
```

---

## 🚨 **故障排除**

### **問題 1: 頁面顯示空白或載入失敗**
```bash
# 檢查核心文件是否存在
cd ~/wakeupmap-pi
ls -la pi.html pi-script.js pi-style.css

# 檢查文件權限
chmod 644 pi.html pi-script.js pi-style.css
chmod 755 api/config/
```

### **問題 2: JavaScript 錯誤**
```bash
# 在瀏覽器控制台檢查錯誤信息
# 常見問題：

# 1. Firebase 配置載入失敗
curl http://localhost:8000/api/config
# 應該返回 Firebase 配置，如果返回錯誤檢查 api/config/index.js

# 2. 地圖載入失敗
# 檢查網路連接，Leaflet 需要從 CDN 載入
```

### **問題 3: 地圖不顯示**
在瀏覽器控制台檢查：
```javascript
// 檢查地圖狀態
console.log('地圖狀態:', {
    leafletLoaded: typeof L !== 'undefined',
    mapInstance: !!window.mainInteractiveMap,
    mapContainer: !!document.getElementById('mainMapContainer')
});

// 如果地圖容器存在但地圖未初始化，手動初始化:
if (typeof L !== 'undefined' && !window.mainInteractiveMap) {
    console.log('嘗試重新初始化地圖...');
    // 可能需要重新載入頁面
}
```

### **問題 4: 功能按鈕無反應**
```javascript
// 檢查主要函數是否載入
console.log({
    startTheDay: typeof window.startTheDay,
    hiddenButtonControl: typeof window.hiddenButtonControl
});

// 如果函數不存在，檢查 pi-script.js 是否正常載入
// 查看瀏覽器網路面板確認文件載入狀態
```

### **問題 5: 網路相關問題**
```bash
# 檢查網路連接
ping 8.8.8.8

# 檢查 DNS 解析
nslookup googleapis.com

# 檢查防火牆設定 (如果使用)
sudo ufw status
```

### **問題 6: GPIO 按鈕無反應** (模式 B 用戶)
```bash
# 1. 檢查 GPIO 控制程式是否運行
ps aux | grep main_web_dsi

# 2. 檢查按鈕接線
# 確認按鈕連接到 GPIO18 和 GND

# 3. 測試 GPIO 權限
sudo usermod -a -G gpio $USER
# 需要重新登入才生效

# 4. 檢查 pigpio 守護程序 (如果使用)
sudo systemctl status pigpiod
sudo systemctl start pigpiod  # 如果未啟動

# 5. 手動測試 GPIO
python3 -c "
import RPi.GPIO as GPIO
GPIO.setmode(GPIO.BCM)
GPIO.setup(18, GPIO.IN, pull_up_down=GPIO.PUD_UP)
print('GPIO18 狀態:', 'HIGH' if GPIO.input(18) else 'LOW')
GPIO.cleanup()
"
```

### **問題 7: Python 依賴安裝失敗**
```bash
# 1. 更新 pip
python3 -m pip install --upgrade pip

# 2. 安裝系統依賴包
sudo apt install python3-dev python3-setuptools -y

# 3. 逐個安裝依賴
cd ~/wakeupmap-pi/raspberrypi-dsi
pip3 install RPi.GPIO
pip3 install selenium
pip3 install pygame
pip3 install psutil

# 4. 如果 selenium 失敗，手動安裝瀏覽器驅動
sudo apt install chromium-browser chromium-chromedriver -y
```

### **問題 8: 回到原始狀態**
```bash
# 如果所有方法都失敗，重新下載:
cd ~
rm -rf wakeupmap-pi-backup
mv wakeupmap-pi wakeupmap-pi-backup  # 備份有問題的版本
git clone https://github.com/yutingcheng/wakeupmap-pi.git
cd wakeupmap-pi
```

---

## 📊 **安裝完成檢查清單**

- [ ] ✅ 專案目錄存在: `~/wakeupmap-pi/`
- [ ] ✅ 核心文件存在: `pi.html`, `pi-script.js`, `pi-style.css`
- [ ] ✅ 城市資料庫存在: `cities_data.json`
- [ ] ✅ API 配置正常: `api/config/index.js` 可訪問
- [ ] ✅ 瀏覽器可正常載入頁面 (無 JavaScript 錯誤)
- [ ] ✅ 可以看到 "PRESS THE BUTTON" 主畫面
- [ ] ✅ 地圖背景正常顯示
- [ ] ✅ 快捷鍵功能正常 (Ctrl+H, ESC 鍵測試)
- [ ] ✅ 手動觸發功能正常 (控制台測試 `window.startTheDay()`)
- [ ] ✅ 隱藏按鈕可點擊 (左上角透明按鈕)

### **GPIO 模式額外檢查** (模式 B 用戶)
- [ ] ✅ `raspberrypi-dsi/` 目錄存在且完整
- [ ] ✅ Python 依賴已安裝 (`pip3 list | grep RPi.GPIO`)
- [ ] ✅ GPIO 權限設定正確 (`groups | grep gpio`)
- [ ] ✅ GPIO 按鈕功能測試正常
- [ ] ✅ 音頻輸出測試正常
- [ ] ✅ GPIO 控制程式可正常啟動 (`python3 main_web_dsi.py --test`)

---

## 🎉 **完成！**

現在你的樹莓派上運行的是**甦醒地圖樹莓派版本**！

### **主要特色**
- 🖥️ **樹莓派最佳化**: 專為 800x480 螢幕設計
- 🎮 **多種控制方式**: 觸控、鍵盤快捷鍵、瀏覽器控制台、GPIO 實體按鈕 (可選)
- 🎨 **像素風格 UI**: 復古科技美學
- 🌍 **全球隨機定位**: 每日不同的甦醒體驗
- 🔊 **多語言語音**: OpenAI TTS 支援 (如有配置)

### **使用方式**
1. **網頁版本**: 開啟瀏覽器訪問 `http://localhost:8000/pi.html`
2. **快捷鍵**: 按 `Ctrl + H` 觸發開始功能
3. **緊急按鈕**: 按 `ESC` 顯示左上角緊急按鈕
4. **隱藏按鈕**: 點擊左上角透明按鈕 (🆘 圖示)
5. **GPIO 按鈕**: 實體按鈕控制 (如已設定模式 B)

### **日常維護**
```bash
# 更新程式碼
cd ~/wakeupmap-pi
git pull origin main

# 修改樣式
nano pi-style.css

# 修改功能邏輯
nano pi-script.js
```

### **需要幫助？**
- 查看瀏覽器控制台 (F12) 獲取錯誤信息
- 執行故障排除步驟
- 檢查網路連接狀態

🍓 **享受你的甦醒地圖樹莓派體驗吧！** ✨