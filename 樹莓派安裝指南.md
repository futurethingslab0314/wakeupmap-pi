# 🍓 Raspberry Pi 甦醒地圖安裝指南

## 接線至raspberry pi：
# 按鈕[+] 接到 GPIO 18 (實體針腳 12) 和 [-] 則接到 GND



### **步驟1：下載或更新專案**
```bash
# 首次安裝，找到自己的branch安裝自己的git：
# 在raspberrypi 上終端機(terminal) 輸入這個指令，將yutingpi <branch名稱>改成 自己的branch，後面網址則是github中depository的連結: 
git clone -b yutingpi --single-branch https://github.com/futurethingslab0314/wakeupmap-pi.git

### **步驟1-1備註：更新專案方式**
# 下載完後，應該在自己的raspberry pi 內可以找到「wakeupmap-pi」的資料夾。
# 終端機(terminal) 內可以確認自己是否在自己的branch內進行後續更新：

cd wakeupmap-pi # 這代表進入到名為wakeupmap-pi的資料夾
git branch # 檢查目前branch叫做什麼
# 輸入完後，應當會告訴你目前的branch為「你的名字，例如yutingpi」。
# 如果不是，則：
git checkout yutingpi #將yutingpi改為自己的branch

# 確定是自己的branch後，若有更新上傳至該branch新的code，可以輸入下方指令，進行更新：

git fetch origin # 更新遠端倉庫資訊
git pull origin yutingpi #然後合併 yutingpi branch 的更新


# 檢查是否成功下載
git log --oneline -3
```

### **步驟 2: 驗證核心文件**
```bash
# 檢查必要文件是否存在
ls -la pi.html pi-script.js pi-style.css

# 應該看到:
# pi.html          - 主介面文件
# pi-script.js     - 核心邏輯腳本  
# pi-style.css     - 樣式文件
# cities_data.json - 城市資料庫

# 檢查 API 配置
ls -la api/config/
```

### **步驟 3: 安裝 環境相關**
```bash
# 更新系統套件
sudo apt update && sudo apt upgrade -y

# 安裝基本 Python 環境
sudo apt install python3 python3-pip python3-venv python3-full -y

# 進入 GPIO 控制程式目錄
cd raspberrypi-dsi

# 創建虛擬環境 (解決 PEP 668 限制)
python3 -m venv venv

# 激活虛擬環境
source venv/bin/activate

# 安裝依賴到虛擬環境
pip install -r requirements.txt

# 檢查安裝是否成功
python -c "import RPi.GPIO, selenium, pygame; print('✅ 主要依賴安裝成功')"


# 檢查並安裝 pigpio (為了讓button可以被偵測到)
pip install pigpio

# 啟動 pigpiod 服務
sudo systemctl start pigpiod
sudo systemctl enable pigpiod

# 停用虛擬環境 (暫時)
deactivate
```

### **步驟 4: 設定 各種API服務的 雲端同步** (必須)

甦醒地圖使用 Firebase 進行雲端資料同步，有兩種配置方式：

#### **本地環境變數設定** 
由於 [Vercel API 受到認證保護] 建議在樹莓派本地設定環境變數：

```bash
# 終端機(terminal)：
cd ~/wakeupmap-pi
nano .env # 創建環境變數文件

# 在這份 .env 文件中，貼上會需要用到的API，例如Firebase, openAI:
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com  
FIREBASE_PRIVATE_KEY=/home/futurethingslab0314/wakeupmap-pi/raspberrypi-dsi/private_key.pem
OPENAI_API_KEY=sk-your-openai-api-key-here # OpenAI API Key (用於高品質 TTS 語音，可選)

# 設定您的用戶名稱 (會影響資料儲存和載入)
USER_NAME=yutingpi  # 改為您想要的用戶名稱，例如：yutingpi, alice, bob 等

# 保存並退出 (Ctrl+X, Y, Enter)


# Firebase由於API很長，為求方便copy and paste，我們可以另外創建私鑰檔案，並讓FIREBASE_PRIVATE_KEY=貼上要連去的私鑰檔案位置：
nano ~/wakeupmap-pi/raspberrypi-dsi/private_key.pem

# 將完整私鑰貼入該空白檔案中 (包含 -----BEGIN PRIVATE KEY----- 和 -----END PRIVATE KEY-----)
# 儲存並退出 (Ctrl+X, Y, Enter)

# 設定檔案權限 (僅擁有者可讀)
chmod 600 ~/wakeupmap-pi/raspberrypi-dsi/private_key.pem


# 載入環境變數
echo 'export $(cat ~/wakeupmap-pi/.env | xargs)' >> ~/.bashrc
source ~/.bashrc

# 可以驗證環境變數是否設定成功
echo "Firebase Project: $FIREBASE_PROJECT_ID"
echo "Private Key Path: $PRIVATE_KEY_PATH"
echo "OpenAI API Key: ${OPENAI_API_KEY:0:20}..."
```

### **步驟 5: 設定 OpenAI TTS 語音** (可選)

如果您將 OpenAI API Key有成功貼至 .env，可以執行以下去調整AI 語音細節：
```bash

cd ~/wakeupmap-pi/raspberrypi-dsi # 進入 GPIO 控制程式目錄
source venv/bin/activate # 使用虛擬環境 (如果之前創建了)
python3 setup_openai_tts.py # 執行 OpenAI TTS 設定工具

# 設定工具會：
# 1. 自動安裝 OpenAI 庫
# 2. 偵測環境變數中的 API Key
# 3. 切換到 OpenAI TTS 引擎
# 4. 測試語音品質

# 停用虛擬環境
deactivate

# 執行 GPIO 控制程式安裝腳本
cd raspberrypi-dsi
chmod +x install_web_dsi.sh
./install_web_dsi.sh # 自動安裝
```

# 或使用自動啟動服務 (如果已設定)
sudo systemctl start wakeupmap

# 按下實體按鈕即可觸發功能
```


### 桌面app設置**
```bash
nano ~/Desktop/WakeUpMap.desktop

# 在這份文件中貼上：

[Desktop Entry]
Version=1.0
Type=Application
Name=WakeUp Map
Comment=Run WakeUpMap Web App
Exec=python3 /home/pi帳號名稱/wakeupmap-pi/raspberrypi-dsi/main_web_dsi.py
Icon=/home/pi帳號名稱/wakeupmap-pi/icon-192x192.png
Terminal=true
Categories=Utility;
Keywords=wakeup;map;raspberry;pi

#儲存離開

chmod +x ~/Desktop/WakeUpMap.desktop #設定執行權限

```

---------------------------------------------------------------------------------------
#想要能夠透過瀏覽器了解問題log，暫時關閉全螢幕的方式：
至raspberrypi-dsi/web_controller_dsi.py中，找到下方程式碼，並臨時隱藏：
        # 全螢幕 kiosk 模式，隱藏瀏覽器分頁和工具列
        options.add_argument('--kiosk')
        options.add_argument('--disable-infobars')
        options.add_argument('--hide-scrollbars')
## 🚨 **故障排除**

### **問題 1: 頁面顯示空白或載入失敗**
```bash
# 檢查核心文件是否存在
cd ~/wakeupmap-pi
ls -la pi.html pi-script.js pi-style.css

# 檢查文件權限
chmod 644 pi.html pi-script.js pi-style.css
chmod 755 api/config/
```

### **問題 2: Firebase 配置錯誤**
```bash
# 1. 檢查環境變數是否正確設定
env | grep FIREBASE

# 2. 檢查 .env 文件格式
cat ~/wakeupmap-pi/.env

# 3. 測試 Firebase 配置載入
curl http://localhost:8000/api/config
# 應該返回包含 projectId 等的 JSON

# 4. 檢查私鑰格式 (常見問題)
# 確保私鑰包含 -----BEGIN PRIVATE KEY----- 和 -----END PRIVATE KEY-----
# 確保沒有額外的空格或換行符問題
```

### **問題 3: JavaScript 錯誤**
```bash
# 在瀏覽器控制台檢查錯誤信息
# 常見問題：

# 1. Firebase 初始化失敗
# 檢查控制台是否有 "Firebase 初始化失敗" 錯誤
# 確認 API 配置端點可訪問

# 2. 地圖載入失敗
# 檢查網路連接，Leaflet 需要從 CDN 載入
```

### **問題 4: 地圖不顯示**
在瀏覽器控制台檢查：
```javascript
// 檢查地圖狀態
console.log('地圖狀態:', {
    leafletLoaded: typeof L !== 'undefined',
    mapInstance: !!window.mainInteractiveMap,
    mapContainer: !!document.getElementById('mainMapContainer')
});

// 如果地圖容器存在但地圖未初始化，手動初始化:
if (typeof L !== 'undefined' && !window.mainInteractiveMap) {
    console.log('嘗試重新初始化地圖...');
    // 可能需要重新載入頁面
}
```

### **問題 5: 功能按鈕無反應**
```javascript
// 檢查主要函數是否載入
console.log({
    startTheDay: typeof window.startTheDay,
    hiddenButtonControl: typeof window.hiddenButtonControl
});

// 如果函數不存在，檢查 pi-script.js 是否正常載入
// 查看瀏覽器網路面板確認文件載入狀態
```

### **問題 6: 網路相關問題**
```bash
# 檢查網路連接
ping 8.8.8.8

# 檢查 DNS 解析
nslookup googleapis.com

# 檢查防火牆設定 (如果使用)
sudo ufw status
```

### **問題 7: GPIO 按鈕無反應** (模式 B 用戶)
```bash
# 1. 檢查 GPIO 控制程式是否運行
ps aux | grep main_web_dsi

# 2. 檢查按鈕接線
# 確認按鈕連接到 GPIO18 和 GND

# 3. 測試 GPIO 權限
sudo usermod -a -G gpio $USER
# 需要重新登入才生效

# 4. 檢查 pigpio 守護程序 (如果使用)
sudo systemctl status pigpiod
sudo systemctl start pigpiod  # 如果未啟動

# 5. 手動測試 GPIO
python3 -c "
import RPi.GPIO as GPIO
GPIO.setmode(GPIO.BCM)
GPIO.setup(18, GPIO.IN, pull_up_down=GPIO.PUD_UP)
print('GPIO18 狀態:', 'HIGH' if GPIO.input(18) else 'LOW')
GPIO.cleanup()
"
```

### **問題 8: Python 依賴安裝失敗**

#### **PEP 668 錯誤 (externally-managed-environment)**
```bash
# 新版 Raspberry Pi OS 需要使用虛擬環境

# 1. 確保已安裝必要系統套件
sudo apt install python3-full python3-venv python3-dev python3-setuptools -y

# 2. 創建並使用虛擬環境
cd ~/wakeupmap-pi/raspberrypi-dsi
python3 -m venv venv
source venv/bin/activate

# 3. 更新虛擬環境中的 pip
pip install --upgrade pip

# 4. 安裝依賴
pip install -r requirements.txt

# 5. 如果個別套件失敗，逐個安裝
pip install RPi.GPIO
pip install selenium
pip install pygame
pip install psutil
pip install requests

# 6. 如果 selenium 失敗，手動安裝瀏覽器驅動
sudo apt install chromium-browser chromium-chromedriver -y
```


### **問題 9: OpenAI TTS 語音問題** (可選功能)

```bash
# 1. 檢查 OpenAI API Key 是否正確
echo "API Key 前綴: ${OPENAI_API_KEY:0:7}"
# 應該顯示: sk-proj 或 sk-

# 2. 測試 OpenAI 連接
cd ~/wakeupmap-pi/raspberrypi-dsi
source venv/bin/activate
python3 -c "
import openai
import os
client = openai.OpenAI(api_key=os.getenv('OPENAI_API_KEY'))
print('✅ OpenAI 連接成功')
"

# 3. 檢查 TTS 設定
cat config.py | grep -A 5 "TTS_CONFIG"

# 4. 重新設定 TTS
python3 setup_openai_tts.py

# 5. 如果沒有 API Key，使用預設語音引擎
# 編輯 config.py，將 engine 改為 'festival'
```

### **問題 10: 回到原始狀態**
```bash
# 如果所有方法都失敗，重新下載:
cd ~
rm -rf wakeupmap-pi-backup
mv wakeupmap-pi wakeupmap-pi-backup  # 備份有問題的版本
git clone https://github.com/yutingcheng/wakeupmap-pi.git
cd wakeupmap-pi
```

---

## 📊 **安裝完成檢查清單**

### **基本安裝檢查**
- [ ] ✅ 專案目錄存在: `~/wakeupmap-pi/`
- [ ] ✅ 核心文件存在: `pi.html`, `pi-script.js`, `pi-style.css`
- [ ] ✅ 城市資料庫存在: `cities_data.json`
- [ ] ✅ API 配置正常: `api/config/index.js` 可訪問

### **Firebase 雲端同步檢查**
- [ ] ✅ Firebase 環境變數已設定 (`echo $FIREBASE_PROJECT_ID`)
- [ ] ✅ Firebase 配置 API 正常 (`curl localhost:8000/api/config`)
- [ ] ✅ Firebase 初始化成功 (瀏覽器控制台無錯誤)
- [ ] ✅ 可從 [Vercel 環境變數](https://vercel.com/future-things-labs-projects/wakeupmap-pi/settings/environment-variables) 取得正確的配置值

### **OpenAI TTS 語音檢查** (可選)
- [ ] ✅ OpenAI API Key 已設定 (`echo $OPENAI_API_KEY`)
- [ ] ✅ OpenAI 庫已安裝 (`python3 -c "import openai; print('✅ OpenAI 庫可用')"`)
- [ ] ✅ TTS 引擎已切換到 OpenAI (查看 `raspberrypi-dsi/config.py`)
- [ ] ✅ 語音測試正常 (如果有按鈕硬體)

### **功能測試檢查**
- [ ] ✅ 瀏覽器可正常載入頁面 (無 JavaScript 錯誤)
- [ ] ✅ 可以看到 "PRESS THE BUTTON" 主畫面
- [ ] ✅ 地圖背景正常顯示
- [ ] ✅ 快捷鍵功能正常 (Ctrl+H, ESC 鍵測試)
- [ ] ✅ 手動觸發功能正常 (控制台測試 `window.startTheDay()`)
- [ ] ✅ 隱藏按鈕可點擊 (左上角透明按鈕)
- [ ] ✅ 資料可正常儲存到 Firebase (觸發一次並檢查 Firestore)

### **GPIO 模式額外檢查** (如使用實體按鈕)
- [ ] ✅ `raspberrypi-dsi/` 目錄存在且完整
- [ ] ✅ 虛擬環境已創建 (`ls raspberrypi-dsi/venv/`)
- [ ] ✅ Python 依賴已安裝 (`source venv/bin/activate && pip list | grep RPi.GPIO`)
- [ ] ✅ GPIO 權限設定正確 (`groups | grep gpio`)
- [ ] ✅ **硬體接線正確**：
  - [ ] 按鈕一端連接到 GPIO 18 (實體針腳 12)
  - [ ] 按鈕另一端連接到 GND (實體針腳 6, 9, 14, 20, 25, 30, 34, 39 任一個)
  - [ ] 使用瞬時按鈕開關，非切換開關
- [ ] ✅ GPIO 按鈕電路測試正常 (使用故障排除的測試程式)
- [ ] ✅ 音頻輸出測試正常
- [ ] ✅ GPIO 控制程式可正常啟動 (使用虛擬環境)

---

## 🎉 **完成！**

現在你的樹莓派上運行的是**甦醒地圖樹莓派版本**！

### **主要特色**
- 🖥️ **樹莓派最佳化**: 專為 800x480 螢幕設計
- 🎮 **多種控制方式**: 觸控、鍵盤快捷鍵、瀏覽器控制台、GPIO 實體按鈕 (可選)
- 🎨 **像素風格 UI**: 復古科技美學
- 🌍 **全球隨機定位**: 每日不同的甦醒體驗
- 🔊 **多語言語音**: OpenAI TTS 支援 (如有配置)

### **使用方式**
1. **網頁版本**: 開啟瀏覽器訪問 `http://localhost:8000/pi.html`
2. **快捷鍵**: 按 `Ctrl + H` 觸發開始功能
3. **緊急按鈕**: 按 `ESC` 顯示左上角緊急按鈕
4. **隱藏按鈕**: 點擊左上角透明按鈕 (🆘 圖示)
5. **GPIO 按鈕**: 實體按鈕控制 (如已設定模式 B)

### **日常維護**
```bash
# 更新程式碼
cd ~/wakeupmap-pi
git pull origin main

# 修改樣式
nano pi-style.css

# 修改功能邏輯
nano pi-script.js
```

### **問題 10: GPIO 實體按鈕無法正常工作** 

```bash
# 1. 檢查按鈕硬體連接
# 確認按鈕已正確連接到 GPIO 18 (實體針腳 12) 和 GND

# 2. 測試 GPIO 按鈕電路
python3 -c "
import RPi.GPIO as GPIO
import time
GPIO.setmode(GPIO.BCM)
GPIO.setup(18, GPIO.IN, pull_up_down=GPIO.PUD_UP)
print('🔘 GPIO 18 按鈕測試 - 請按下按鈕...')
for i in range(20):
    if GPIO.input(18) == GPIO.LOW:
        print('✅ 按鈕觸發成功！電路連接正常')
        break
    time.sleep(0.5)
    print(f'等待中... ({i+1}/20)')
else:
    print('❌ 未偵測到按鈕觸發，請檢查接線')
GPIO.cleanup()
"

# 3. 檢查 GPIO 控制程式狀態
cd ~/wakeupmap-pi/raspberrypi-dsi
source venv/bin/activate
python3 main_web_dsi.py --test  # 測試模式

# 4. 常見接線問題排除：
# - 確認按鈕連接到 GPIO 18 (針腳 12)，不是其他針腳
# - 確認另一端連接到 GND (針腳 6, 9, 14, 20, 25, 30, 34, 39 任一個)
# - 確認使用瞬時按鈕 (momentary switch)，不是切換開關 (toggle switch)
# - 確認接線穩固，沒有鬆脫

# 5. 檢查權限問題
sudo usermod -a -G gpio $USER  # 將用戶加入 gpio 群組
# 重新登入後再測試

deactivate
```

🍓 **享受你的甦醒地圖樹莓派體驗吧！** ✨